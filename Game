<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pilih Mode Game</title>
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f5f5f5;
        }
        .container {
            text-align: center;
        }
        .game-mode {
            position: fixed;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            gap: 20px;
        }
        .mode-box {
            padding: 20px;
            background-color: cyan;
            border-radius: 10px;
            cursor: pointer;
            transition: 0.3s;
            color: white;
            font-weight: bold;
        }
        .mode-box:hover {
            background-color: lightblue;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="w3-padding w3-cyan w3-round-large">
            <h2>PILIH MODE GAME</h2>
        </div>
        <div class="game-mode">
            <div class="mode-box" onclick="selectMode(2)">2 PLAYER</div>
            <div class="mode-box" onclick="selectMode(3)">3 PLAYER</div>
            <div class="mode-box" onclick="selectMode(4)">4 PLAYER</div>
        </div>
    </div>
    
    <script>
        function selectMode(players) {
            alert("Mode " + players + " pemain dipilih");
            // Tambahkan logika untuk memulai game dengan mode yang dipilih
        }
    </script>
</body>
</html>
